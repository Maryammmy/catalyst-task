export interface IUser {
  id: number;
  name: string;
  email: string;
  phone: string;
  role: string;
  profile_image: string;
  intro_video: string;
}
export interface User {
  name: string;
  email: string;
  phone: string;
  role: string;
  profile_image: string;
  intro_video: string;
}
